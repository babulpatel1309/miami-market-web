// Open/closed status derived from the current time.

export type OpenStatus = {
  statusOpen: boolean;
  statusLabel: string;
  statusSub: string;
  dayName: string;
  signWord: string;
  statusDot: string;
  todayIdx: number;
};
